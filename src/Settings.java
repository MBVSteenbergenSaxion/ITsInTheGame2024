public class Settings {

    /**
     * - Settings class to set the width and height of the canvas.
     * - Sets the button width and height relative to canvas width and height.
     * - Sets the delay in millisecond for the frame-time
     * - Sets the fontsize
     * */
    public static int height = 768;
    public static int width = 1024;
    public static int ms = -1000;

    public static int buttonWidth = width / 3;
    public static int buttonHeight = height / 10;
    public static int fontSize = 25;

}
